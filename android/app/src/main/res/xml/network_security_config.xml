<?xml version="1.0" encoding="utf-8"?>
<!-- Network security config for Smriti
     Only allows HTTPS connections. HTTP is blocked.
     Hugging Face model downloads use HTTPS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Explicitly allow HTTPS to Hugging Face for model downloads -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">huggingface.co</domain>
        <domain includeSubdomains="true">cdn-lfs.huggingface.co</domain>
    </domain-config>
</network-security-config>
